# ELK_Docker-compose_setup

# read this first : 
    start with configuring the elasticsearch container first !
    after that do changes on kibana.yml file on kibana container using vi editor ! 
    that's all .
